class Combat

#holy crap, accidentally nuked my combat class.

  def initialize( liveArray, deadArray, sparBool )
    fight( liveArray, deadArray, sparBool  )
  end

  def fight
    if liveArray.length <= 1
      puts 'There needs to be atleast two living combatants to fight.'
    else
      puts '<<The Heroes>>'
      puts liveArray.each.show_name
#      puts 'Select first combatant'
#	input = gets.chomp
    end
  end
end
